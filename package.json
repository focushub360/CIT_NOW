{
  "name": "citnow-monorepo-root",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo root config to automate cloud deployment pipelines",
  "scripts": {
    "prebuild": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend && mkdir -p build && cp -R frontend/build/* build/"
  }
}
